version: 1.0.{build}-{branch}

clone_folder: c:\project\stumpless

configuration:
- Release
- Debug

image:
- Visual Studio 2019
- Visual Studio 2017
- Visual Studio 2015

platform:
- x64

environment:
  matrix:
  - arch: Win64
    network_targets_enabled: OFF
  - arch: Win64
    network_targets_enabled: ON

init:
- set arch=
- if "%arch%"=="Win64" ( set arch= Win64)
- if "%APPVEYOR_BUILD_WORKER_IMAGE%"=="Visual Studio 2019" ( set generator="Visual Studio 16 2019" )
- if "%APPVEYOR_BUILD_WORKER_IMAGE%"=="Visual Studio 2017" ( set generator="Visual Studio 15 2017%arch%" )
- if "%APPVEYOR_BUILD_WORKER_IMAGE%"=="Visual Studio 2015" ( set generator="Visual Studio 14 2015%arch%" )

before_build:
- cmd: |-
    echo %PATH%
    where mc.exe
    mkdir build
    cd build
    cmake .. -G %generator% -DCMAKE_BUILD_TYPE=%configuration% -DENABLE_NETWORK_TARGETS=%network_targets_enabled%

build:
  project: c:\project\stumpless\build\stumpless.vcxproj

test_script:
  - set CTEST_OUTPUT_ON_FAILURE=1
  - msbuild c:\project\stumpless\build\check.vcxproj
  - msbuild c:\project\stumpless\build\examples.vcxproj
